# Backend
- The behind the scenes...

## React Set up (don't do this if there is already a little villagehub folder): 
# Backend setup (Node.js, NOT Python)
cd backend
npm init -y
npm install express cors dotenv firebase-admin nodemailer node-cron

### Work here only if you need node.js!
